meta {
  name: Create
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/{{API_VERSION}}/users/1/blocks
  body: json
  auth: inherit
}

headers {
  Origin: {{FRONTEND_ORIGIN}}
}

body:json {
  {
      "blocked_id": 2
  }
}
