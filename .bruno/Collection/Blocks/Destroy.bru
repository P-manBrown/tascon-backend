meta {
  name: Destroy
  type: http
  seq: 3
}

delete {
  url: {{BASE_URL}}/{{API_VERSION}}/blocks/1
  body: none
  auth: bearer
}

headers {
  Origin: {{FRONTEND_ORIGIN}}
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}
