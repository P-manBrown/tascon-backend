meta {
  name: Delete
  type: http
  seq: 7
}

delete {
  url: {{BASE_URL}}/{{API_VERSION}}/tasks/1
  body: none
  auth: inherit
}

headers {
  Origin: {{FRONTEND_ORIGIN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
