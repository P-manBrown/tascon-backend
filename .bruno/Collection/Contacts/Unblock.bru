meta {
  name: Unblock
  type: http
  seq: 5
}

patch {
  url: {{BASE_URL}}/{{API_VERSION}}/contacts/1/unblock
  body: none
  auth: bearer
}

headers {
  Origin: {{FRONTEND_ORIGIN}}
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}
