meta {
  name: Blocked
  type: http
  seq: 6
}

get {
  url: {{BASE_URL}}/{{API_VERSION}}/users/1/contacts/blocked?page=1
  body: none
  auth: bearer
}

params:query {
  page: 1
}

headers {
  Origin: {{FRONTEND_ORIGIN}}
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}
