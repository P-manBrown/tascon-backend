meta {
  name: Block
  type: http
  seq: 4
}

patch {
  url: {{BASE_URL}}/{{API_VERSION}}/contacts/1/block
  body: none
  auth: bearer
}

headers {
  Origin: {{FRONTEND_ORIGIN}}
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}
